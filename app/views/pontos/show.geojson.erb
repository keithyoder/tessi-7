{
    "type": "FeatureCollection",
    "crs": {
        "type": "name",
        "properties": {
            "name": "urn:ogc:def:crs:OGC:1.3:CRS84"
        }
    },
    "features": [
        <%= @ponto.conexoes.includes(:pessoa).georeferencidas.map(&:as_geojson).join(',').html_safe %>
    ]
}