<p id="notice"><%= notice %></p>

<h1>Conexao Enviar Atributos</h1>

<table>
  <thead>
    <tr>
      <th>Conexao</th>
      <th>Atributo</th>
      <th>Op</th>
      <th>Valor</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @conexao_enviar_atributos.each do |conexao_enviar_atributo| %>
      <tr>
        <td><%= conexao_enviar_atributo.conexao %></td>
        <td><%= conexao_enviar_atributo.atributo %></td>
        <td><%= conexao_enviar_atributo.op %></td>
        <td><%= conexao_enviar_atributo.valor %></td>
        <td><%= link_to 'Show', conexao_enviar_atributo %></td>
        <td><%= link_to 'Edit', edit_conexao_enviar_atributo_path(conexao_enviar_atributo) %></td>
        <td><%= link_to 'Destroy', conexao_enviar_atributo, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Conexao Enviar Atributo', new_conexao_enviar_atributo_path %>
